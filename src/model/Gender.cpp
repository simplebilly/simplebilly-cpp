/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.25.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CppRestOpenAPIClient/model/Gender.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

Gender::eGender toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("male")))
        return Gender::eGender::MALE;
    if (val == utility::conversions::to_string_t(_XPLATSTR("female")))
        return Gender::eGender::FEMALE;
    if (val == utility::conversions::to_string_t(_XPLATSTR("diverse")))
        return Gender::eGender::DIVERSE;
    return {};
}

EnumUnderlyingType fromEnum(Gender::eGender e)
{
    switch (e)
    {
    case Gender::eGender::MALE:
        return _XPLATSTR("male");
    case Gender::eGender::FEMALE:
        return _XPLATSTR("female");
    case Gender::eGender::DIVERSE:
        return _XPLATSTR("diverse");
    default:
        break;
    }
    return {};
}
}

Gender::Gender()
{
}

Gender::~Gender()
{
}

void Gender::validate()
{
    // TODO: implement validation
}

web::json::value Gender::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool Gender::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void Gender::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool Gender::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

Gender::eGender Gender::getValue() const
{
   return m_value;
}

void Gender::setValue(Gender::eGender const value)
{
   m_value = value;
}

Gender::Gender(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}
}
}


