/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.25.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CppRestOpenAPIClient/model/SyncStatus.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

SyncStatus::eSyncStatus toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("pending")))
        return SyncStatus::eSyncStatus::PENDING;
    if (val == utility::conversions::to_string_t(_XPLATSTR("connected")))
        return SyncStatus::eSyncStatus::CONNECTED;
    if (val == utility::conversions::to_string_t(_XPLATSTR("success")))
        return SyncStatus::eSyncStatus::SUCCESS;
    if (val == utility::conversions::to_string_t(_XPLATSTR("error")))
        return SyncStatus::eSyncStatus::ERROR;
    return {};
}

EnumUnderlyingType fromEnum(SyncStatus::eSyncStatus e)
{
    switch (e)
    {
    case SyncStatus::eSyncStatus::PENDING:
        return _XPLATSTR("pending");
    case SyncStatus::eSyncStatus::CONNECTED:
        return _XPLATSTR("connected");
    case SyncStatus::eSyncStatus::SUCCESS:
        return _XPLATSTR("success");
    case SyncStatus::eSyncStatus::ERROR:
        return _XPLATSTR("error");
    default:
        break;
    }
    return {};
}
}

SyncStatus::SyncStatus()
{
}

SyncStatus::~SyncStatus()
{
}

void SyncStatus::validate()
{
    // TODO: implement validation
}

web::json::value SyncStatus::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool SyncStatus::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void SyncStatus::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool SyncStatus::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

SyncStatus::eSyncStatus SyncStatus::getValue() const
{
   return m_value;
}

void SyncStatus::setValue(SyncStatus::eSyncStatus const value)
{
   m_value = value;
}

SyncStatus::SyncStatus(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}
}
}


