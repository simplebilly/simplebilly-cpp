/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.25.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CppRestOpenAPIClient/model/CheckStatus.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

CheckStatus::eCheckStatus toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("Ok")))
        return CheckStatus::eCheckStatus::OK;
    if (val == utility::conversions::to_string_t(_XPLATSTR("Warning")))
        return CheckStatus::eCheckStatus::WARNING;
    if (val == utility::conversions::to_string_t(_XPLATSTR("Error")))
        return CheckStatus::eCheckStatus::ERROR;
    return {};
}

EnumUnderlyingType fromEnum(CheckStatus::eCheckStatus e)
{
    switch (e)
    {
    case CheckStatus::eCheckStatus::OK:
        return _XPLATSTR("Ok");
    case CheckStatus::eCheckStatus::WARNING:
        return _XPLATSTR("Warning");
    case CheckStatus::eCheckStatus::ERROR:
        return _XPLATSTR("Error");
    default:
        break;
    }
    return {};
}
}

CheckStatus::CheckStatus()
{
}

CheckStatus::~CheckStatus()
{
}

void CheckStatus::validate()
{
    // TODO: implement validation
}

web::json::value CheckStatus::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool CheckStatus::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void CheckStatus::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool CheckStatus::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

CheckStatus::eCheckStatus CheckStatus::getValue() const
{
   return m_value;
}

void CheckStatus::setValue(CheckStatus::eCheckStatus const value)
{
   m_value = value;
}

CheckStatus::CheckStatus(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}
}
}


