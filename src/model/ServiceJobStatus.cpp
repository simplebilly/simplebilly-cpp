/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.25.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CppRestOpenAPIClient/model/ServiceJobStatus.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

ServiceJobStatus::eServiceJobStatus toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("pending")))
        return ServiceJobStatus::eServiceJobStatus::PENDING;
    if (val == utility::conversions::to_string_t(_XPLATSTR("assigned")))
        return ServiceJobStatus::eServiceJobStatus::ASSIGNED;
    if (val == utility::conversions::to_string_t(_XPLATSTR("en_route")))
        return ServiceJobStatus::eServiceJobStatus::EN_ROUTE;
    if (val == utility::conversions::to_string_t(_XPLATSTR("in_progress")))
        return ServiceJobStatus::eServiceJobStatus::IN_PROGRESS;
    if (val == utility::conversions::to_string_t(_XPLATSTR("completed")))
        return ServiceJobStatus::eServiceJobStatus::COMPLETED;
    if (val == utility::conversions::to_string_t(_XPLATSTR("cancelled")))
        return ServiceJobStatus::eServiceJobStatus::CANCELLED;
    return {};
}

EnumUnderlyingType fromEnum(ServiceJobStatus::eServiceJobStatus e)
{
    switch (e)
    {
    case ServiceJobStatus::eServiceJobStatus::PENDING:
        return _XPLATSTR("pending");
    case ServiceJobStatus::eServiceJobStatus::ASSIGNED:
        return _XPLATSTR("assigned");
    case ServiceJobStatus::eServiceJobStatus::EN_ROUTE:
        return _XPLATSTR("en_route");
    case ServiceJobStatus::eServiceJobStatus::IN_PROGRESS:
        return _XPLATSTR("in_progress");
    case ServiceJobStatus::eServiceJobStatus::COMPLETED:
        return _XPLATSTR("completed");
    case ServiceJobStatus::eServiceJobStatus::CANCELLED:
        return _XPLATSTR("cancelled");
    default:
        break;
    }
    return {};
}
}

ServiceJobStatus::ServiceJobStatus()
{
}

ServiceJobStatus::~ServiceJobStatus()
{
}

void ServiceJobStatus::validate()
{
    // TODO: implement validation
}

web::json::value ServiceJobStatus::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool ServiceJobStatus::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void ServiceJobStatus::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool ServiceJobStatus::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

ServiceJobStatus::eServiceJobStatus ServiceJobStatus::getValue() const
{
   return m_value;
}

void ServiceJobStatus::setValue(ServiceJobStatus::eServiceJobStatus const value)
{
   m_value = value;
}

ServiceJobStatus::ServiceJobStatus(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}
}
}


