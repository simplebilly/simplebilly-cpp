/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.25.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CppRestOpenAPIClient/model/SupportTicketStatus.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

SupportTicketStatus::eSupportTicketStatus toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("open")))
        return SupportTicketStatus::eSupportTicketStatus::OPEN;
    if (val == utility::conversions::to_string_t(_XPLATSTR("awaiting_reply")))
        return SupportTicketStatus::eSupportTicketStatus::AWAITING_REPLY;
    if (val == utility::conversions::to_string_t(_XPLATSTR("resolved")))
        return SupportTicketStatus::eSupportTicketStatus::RESOLVED;
    if (val == utility::conversions::to_string_t(_XPLATSTR("closed")))
        return SupportTicketStatus::eSupportTicketStatus::CLOSED;
    return {};
}

EnumUnderlyingType fromEnum(SupportTicketStatus::eSupportTicketStatus e)
{
    switch (e)
    {
    case SupportTicketStatus::eSupportTicketStatus::OPEN:
        return _XPLATSTR("open");
    case SupportTicketStatus::eSupportTicketStatus::AWAITING_REPLY:
        return _XPLATSTR("awaiting_reply");
    case SupportTicketStatus::eSupportTicketStatus::RESOLVED:
        return _XPLATSTR("resolved");
    case SupportTicketStatus::eSupportTicketStatus::CLOSED:
        return _XPLATSTR("closed");
    default:
        break;
    }
    return {};
}
}

SupportTicketStatus::SupportTicketStatus()
{
}

SupportTicketStatus::~SupportTicketStatus()
{
}

void SupportTicketStatus::validate()
{
    // TODO: implement validation
}

web::json::value SupportTicketStatus::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool SupportTicketStatus::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void SupportTicketStatus::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool SupportTicketStatus::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

SupportTicketStatus::eSupportTicketStatus SupportTicketStatus::getValue() const
{
   return m_value;
}

void SupportTicketStatus::setValue(SupportTicketStatus::eSupportTicketStatus const value)
{
   m_value = value;
}

SupportTicketStatus::SupportTicketStatus(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}
}
}


