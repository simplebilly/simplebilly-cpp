/**
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * The version of the OpenAPI document: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI-Generator 7.25.0.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */



#include "CppRestOpenAPIClient/model/InstrumentType.h"

namespace org {
namespace openapitools {
namespace client {
namespace model {

namespace
{
using EnumUnderlyingType = utility::string_t;

InstrumentType::eInstrumentType toEnum(const EnumUnderlyingType& val)
{
    if (val == utility::conversions::to_string_t(_XPLATSTR("typisch")))
        return InstrumentType::eInstrumentType::TYPISCH;
    if (val == utility::conversions::to_string_t(_XPLATSTR("atypisch")))
        return InstrumentType::eInstrumentType::ATYPISCH;
    return {};
}

EnumUnderlyingType fromEnum(InstrumentType::eInstrumentType e)
{
    switch (e)
    {
    case InstrumentType::eInstrumentType::TYPISCH:
        return _XPLATSTR("typisch");
    case InstrumentType::eInstrumentType::ATYPISCH:
        return _XPLATSTR("atypisch");
    default:
        break;
    }
    return {};
}
}

InstrumentType::InstrumentType()
{
}

InstrumentType::~InstrumentType()
{
}

void InstrumentType::validate()
{
    // TODO: implement validation
}

web::json::value InstrumentType::toJson() const
{
    auto val = fromEnum(m_value);
    return web::json::value(val);
}

bool InstrumentType::fromJson(const web::json::value& val)
{
    m_value = toEnum(val.as_string());
    return true;
}

void InstrumentType::toMultipart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix) const
{
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }

    auto e = fromEnum(m_value);
    multipart->add(ModelBase::toHttpContent(namePrefix, e));
}

bool InstrumentType::fromMultiPart(std::shared_ptr<MultipartFormData> multipart, const utility::string_t& prefix)
{
    bool ok = true;
    utility::string_t namePrefix = prefix;
    if (!namePrefix.empty() && namePrefix.back() != _XPLATSTR('.'))
    {
        namePrefix.push_back(_XPLATSTR('.'));
    }
    {
        EnumUnderlyingType e;
        ok = ModelBase::fromHttpContent(multipart->getContent(namePrefix), e);
        if (ok)
        {
            auto v = toEnum(e);
            setValue(v);
        }
    }
    return ok;
}

InstrumentType::eInstrumentType InstrumentType::getValue() const
{
   return m_value;
}

void InstrumentType::setValue(InstrumentType::eInstrumentType const value)
{
   m_value = value;
}

InstrumentType::InstrumentType(utility::string_t str){
    setValue( strToEnumMap[str] );
}

}
}
}
}


